# System Blueprint - Body Hack

> 系統藍圖，定義資料模型、模組規格、API 設計

---

## 1. 系統總覽

```
┌─────────────────────────────────────────────────────────────┐
│                      Body Hack 系統                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │           Module A: Streamlit App                    │   │
│  │                                                      │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐            │   │
│  │  │  physio  │ │  events  │ │ insights │            │   │
│  │  │  生理數據 │ │ 事件記錄  │ │ AI 洞察  │            │   │
│  │  └──────────┘ └──────────┘ └──────────┘            │   │
│  │  ┌──────────┐ ┌──────────┐                         │   │
│  │  │  goals   │ │  export  │                         │   │
│  │  │ 目標管理  │ │   匯出   │                         │   │
│  │  └──────────┘ └──────────┘                         │   │
│  └─────────────────────┬───────────────────────────────┘   │
│                        │                                    │
│                        ▼                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │           Module B: Google Sheets                    │   │
│  │                                                      │   │
│  │  ┌────────┐ ┌─────────────────┐ ┌──────────────┐   │   │
│  │  │ Physio │ │ Structured_Events│ │ Simple_Events│   │   │
│  │  └────────┘ └─────────────────┘ └──────────────┘   │   │
│  │  ┌──────────┐ ┌───────┐                            │   │
│  │  │ Insights │ │ Goals │                            │   │
│  │  └──────────┘ └───────┘                            │   │
│  └─────────────────────┬───────────────────────────────┘   │
│                        │                                    │
│                        ▼                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │           Module C: 週期檢視 Skill                   │   │
│  │           （匯出 CSV + Prompt → Claude.ai）          │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 資料模型

### 2.1 Sheet: Physio（生理數據）

時序列數據，來自各種量測裝置。

| 欄位 | 型別 | 說明 | 範例 |
|------|------|------|------|
| id | string | UUID，自動生成 | `a1b2c3d4` |
| datetime | string | ISO 8601，Asia/Taipei | `2025-01-15T14:30:00` |
| date | string | 日期，方便篩選 | `2025-01-15` |
| time | string | 時間 | `14:30` |
| source | string | 資料來源 | `cgm` / `garmin` / `bp_monitor` / `body_scale` / `manual` |
| blood_glucose | number | 血糖 (mg/dL) | `105` |
| glucose_trend | string | 血糖趨勢 | `→` / `↑` / `↓` / `↑↑` / `↓↓` |
| weight | number | 體重 (kg) | `72.5` |
| body_fat | number | 體脂率 (%) | `23.5` |
| systolic | number | 收縮壓 (mmHg) | `120` |
| diastolic | number | 舒張壓 (mmHg) | `80` |
| heart_rate | number | 心率 (bpm) | `72` |
| hrv | number | HRV (ms) | `42` |
| sleep_score | number | 睡眠分數 | `85` |
| steps | number | 步數（日結） | `8500` |
| floors | number | 樓層數（日結） | `12` |
| note | string | 備註 | `LibreLink 截圖` |

**設計說明**：
- 一筆記錄只填有值的欄位，其他留空
- `source` 欄位用於識別資料來源，支援 OCR prompt 選擇
- `date` 欄位冗餘但方便日期篩選

---

### 2.2 Sheet: Structured_Events（結構化事件）

飲食與運動，有專屬欄位的事件。

| 欄位 | 型別 | 說明 | 範例 |
|------|------|------|------|
| id | string | UUID | `e1f2g3h4` |
| datetime | string | ISO 8601 | `2025-01-15T12:30:00` |
| date | string | 日期 | `2025-01-15` |
| time | string | 時間 | `12:30` |
| category | string | 大類別 | `meal` / `exercise` / `water` |
| sub_category | string | 子類別 | `正餐` / `點心` / `散步` / `重訓` |
| content | string | 內容描述 | `7-11 雞胸肉便當` |
| starch_level | string | 澱粉量（飲食用） | `無` / `少` / `中` / `多` |
| is_alcohol | boolean | 是否含酒精 | `false` |
| duration_min | number | 時長（運動用） | `30` |
| intensity | string | 強度（運動用） | `低` / `中` / `高` |
| volume_ml | number | 水量（飲水用） | `500` |
| note | string | 備註 | `餐後散步 20 分鐘` |

**設計說明**：
- `category` 控制哪些欄位有意義
- 飲食：用 `sub_category`（正餐/點心）、`content`、`starch_level`、`is_alcohol`
- 運動：用 `sub_category`（運動類型）、`duration_min`、`intensity`
- 飲水：用 `volume_ml`

---

### 2.3 Sheet: Simple_Events（簡易事件）

次數型或質性描述的事件，結構簡單、類別可擴充。

| 欄位 | 型別 | 說明 | 範例 |
|------|------|------|------|
| id | string | UUID | `s1t2u3v4` |
| datetime | string | ISO 8601 | `2025-01-15T15:00:00` |
| date | string | 日期 | `2025-01-15` |
| time | string | 時間 | `15:00` |
| category | string | 類別（可動態新增） | `吸菸` / `飲酒` / `情緒` / `壓力` / `自訂` |
| description | string | 描述 | `下午會議後壓力大` |
| note | string | 備註 | `` |

**設計說明**：
- `category` 由使用者輸入，App 記錄用過的類別做 autocomplete
- 新類別直接打字即可，無需預先定義

---

### 2.4 Sheet: Insights（AI 洞察）

AI 產出的洞察紀錄。

| 欄位 | 型別 | 說明 | 範例 |
|------|------|------|------|
| id | string | UUID | `i1j2k3l4` |
| datetime | string | 產出時間 | `2025-01-15T12:45:00` |
| date | string | 日期 | `2025-01-15` |
| type | string | 洞察類型 | `realtime` / `weekly` |
| trigger_summary | string | 觸發事件摘要 | `午餐：7-11 雞胸肉便當` |
| context_summary | string | 脈絡摘要（餵給 AI 的摘要） | `今日已記錄：早餐...血糖...` |
| ai_response | string | AI 回應內容 | `這餐搭配不錯...` |
| model | string | 使用的模型 | `gemini-1.5-flash` |
| note | string | 備註 | `` |

---

### 2.5 Sheet: Goals（目標）

使用者設定的健康目標。

| 欄位 | 型別 | 說明 | 範例 |
|------|------|------|------|
| id | string | UUID | `g1h2i3j4` |
| metric | string | 指標名稱 | `體脂率` / `血糖TIR` / `體重` |
| current_value | number | 目前值 | `25.0` |
| target_value | number | 目標值 | `20.0` |
| unit | string | 單位 | `%` / `kg` / `mg/dL` |
| target_date | string | 目標日期 | `2025-06-30` |
| status | string | 狀態 | `active` / `achieved` / `paused` |
| created_at | string | 建立時間 | `2025-01-01T00:00:00` |
| updated_at | string | 更新時間 | `2025-01-15T00:00:00` |
| note | string | 備註 | `三個月減脂計畫` |

---

## 3. Module A: Streamlit App

### 3.1 頁面結構

```
app.py (主入口)
│
├── 📊 首頁 Dashboard
│   └── 今日摘要、最近記錄、快速入口
│
├── 📝 記錄
│   ├── 生理數據（OCR / 手動）
│   ├── 飲食記錄
│   ├── 運動記錄
│   ├── 飲水記錄
│   └── 簡易事件
│
├── 🤖 AI 洞察
│   ├── 即時回饋（當餐）
│   └── 週回顧
│
├── 🎯 目標管理
│   ├── 檢視目標
│   ├── 新增/編輯目標
│   └── 更新目前值
│
└── 📤 匯出
    └── 選擇區間 → 產出 CSV + Prompt
```

### 3.2 Feature: physio（生理數據）

#### 功能規格

| 功能 | 說明 |
|------|------|
| OCR 輸入 | 選擇來源 → 上傳截圖 → Gemini OCR → 預覽確認 → 存入 |
| 手動輸入 | 表單填寫 → 存入 |
| 來源選項 | CGM / Garmin / 血壓計 / 體脂計 / 手動 |

#### OCR 流程

```
使用者選擇來源類型
    ↓
上傳截圖
    ↓
根據來源類型，選擇對應 OCR prompt
    ↓
呼叫 Gemini API
    ↓
解析 JSON 回應
    ↓
顯示預覽，使用者確認或修改
    ↓
確認後寫入 Physio Sheet
```

### 3.3 Feature: events（事件記錄）

#### 結構化事件（飲食/運動/飲水）

| 事件類型 | 表單欄位 |
|----------|----------|
| 飲食 | 時間、餐別（正餐/點心）、內容、澱粉量、是否含酒、備註 |
| 運動 | 時間、運動類型、時長、強度、備註 |
| 飲水 | 時間、水量、備註 |

#### 簡易事件

| 欄位 | 輸入方式 |
|------|----------|
| 類別 | 下拉選單 + 可輸入新類別（combobox） |
| 描述 | 文字輸入 |
| 備註 | 文字輸入（選填） |

**類別建議清單**（從 Simple_Events Sheet 動態讀取已用過的類別）

### 3.4 Feature: insights（AI 洞察）

#### 即時回饋（Realtime）

**觸發時機**：使用者記錄完事件後，按「取得 AI 回饋」按鈕

**Context 組成**：
- 今日已記錄的所有事件（時間排序）
- 今日生理數據
- 當前目標（從 Goals Sheet 讀取 active 狀態的目標）
- 剛才記錄的事件（作為焦點）

**AI 角色**：教練

**回應重點**：
- 這個行為/餐食的評估
- 與今日脈絡的關聯
- 下一步建議（如：下一餐注意事項）

#### 週回顧（Weekly）

**觸發時機**：使用者主動點擊「週回顧」

**Context 組成**：
- 過去 7 天所有記錄（Physio + Events）
- 過去 7 天 AI 洞察紀錄
- 當前目標

**AI 角色**：檢討夥伴

**回應重點**：
- 這週做得好的行為模式
- 需要調整的部分
- 具體的下週建議

### 3.5 Feature: goals（目標管理）

| 功能 | 說明 |
|------|------|
| 檢視目標 | 列出所有 active 目標，顯示進度 |
| 新增目標 | 表單：指標、目前值、目標值、目標日期 |
| 編輯目標 | 修改目標值、日期、狀態 |
| 更新目前值 | 快速更新（如：今天量了體脂，更新目前值） |

### 3.6 Feature: export（匯出）

#### 匯出內容

| 項目 | 說明 |
|------|------|
| 資料範圍 | 使用者選擇起迄日期（預設：近 3 個月） |
| 檔案格式 | CSV（UTF-8 with BOM，Excel 友善） |
| 檔案內容 | Physio + Structured_Events + Simple_Events 合併，依時間排序 |
| 啟動 Prompt | 自動產生，包含目標與資料摘要 |

#### 匯出 CSV 欄位

```
datetime, date, time, record_type, source, category, sub_category, content, blood_glucose, glucose_trend, weight, body_fat, systolic, diastolic, heart_rate, hrv, sleep_score, steps, floors, starch_level, is_alcohol, duration_min, intensity, volume_ml, description, note
```

- `record_type`：`physio` / `structured_event` / `simple_event`
- 不同類型的記錄，不適用的欄位留空

#### 啟動 Prompt 模板

```
我想進行健康數據的週期檢視。

【當前目標】
{goals_list}

【資料概況】
- 資料區間：{start_date} ~ {end_date}
- 總天數：{total_days} 天
- 記錄筆數：{record_count} 筆
- 平均每日記錄：{avg_per_day} 筆

【資料摘要】
- 血糖記錄：{glucose_count} 筆，平均 {glucose_avg} mg/dL
- 體重記錄：{weight_count} 筆，區間 {weight_min}~{weight_max} kg
- 飲食記錄：{meal_count} 筆
- 運動記錄：{exercise_count} 筆

請根據附件的 CSV 資料，分析我的健康趨勢，並討論：
1. 距離目標的進展如何？
2. 有哪些正向的行為模式值得維持？
3. 有哪些需要調整的部分？
4. 接下來的策略建議？
```

---

## 4. OCR Prompt 模板

### 4.1 通用結構

```
你是一個健康數據 OCR 助手。
請從這張 {source_type} 的截圖中擷取以下數值。

【要擷取的欄位】
{field_list}

【輸出格式】
請以 JSON 格式回應，例如：
{example_json}

【注意事項】
- 如果某欄位無法辨識，填入 null
- 數值不要包含單位
- 確保數值為數字型別，不是字串
```

### 4.2 CGM（亞培 LibreLink）

```
你是一個健康數據 OCR 助手。
請從這張亞培 LibreLink CGM App 的截圖中擷取血糖數據。

【要擷取的欄位】
- blood_glucose: 當前血糖值 (mg/dL)
- glucose_trend: 趨勢箭頭（→ 平穩 / ↑ 緩升 / ↓ 緩降 / ↑↑ 快升 / ↓↓ 快降）

【輸出格式】
{
  "blood_glucose": 105,
  "glucose_trend": "→"
}

【注意事項】
- blood_glucose 應為整數
- glucose_trend 用箭頭符號表示
- 如果無法辨識，填入 null
```

### 4.3 Garmin Connect

```
你是一個健康數據 OCR 助手。
請從這張 Garmin Connect App 的截圖中擷取健康數據。

【要擷取的欄位】
- heart_rate: 靜息心率 (bpm)
- hrv: HRV/心率變異（如有顯示）
- sleep_score: 睡眠分數（如有顯示）
- steps: 步數（如有顯示）
- floors: 樓層數（如有顯示）

【輸出格式】
{
  "heart_rate": 62,
  "hrv": 45,
  "sleep_score": 82,
  "steps": 8500,
  "floors": 12
}

【注意事項】
- 只擷取畫面上有顯示的數值
- 沒有顯示的欄位填 null
- 數值為整數
```

### 4.4 血壓計

```
你是一個健康數據 OCR 助手。
請從這張血壓計螢幕的照片中擷取血壓數據。

【要擷取的欄位】
- systolic: 收縮壓 (mmHg)，通常是較大的數字
- diastolic: 舒張壓 (mmHg)，通常是較小的數字
- heart_rate: 脈搏/心率 (bpm)，如果有顯示

【輸出格式】
{
  "systolic": 120,
  "diastolic": 80,
  "heart_rate": 72
}

【注意事項】
- 收縮壓通常在上方或左邊，數值較大
- 舒張壓通常在下方或右邊，數值較小
- 如果無法辨識，填入 null
```

### 4.5 體脂計

```
你是一個健康數據 OCR 助手。
請從這張體脂計螢幕的照片中擷取身體組成數據。

【要擷取的欄位】
- weight: 體重 (kg)
- body_fat: 體脂率 (%)
- 其他有顯示的數值也可以一併擷取

【輸出格式】
{
  "weight": 72.5,
  "body_fat": 23.5
}

【注意事項】
- weight 可以有一位小數
- body_fat 可以有一位小數
- 如果無法辨識，填入 null
```

---

## 5. AI 洞察 Prompt 框架

### 5.1 即時回饋（Realtime）

```
你是一位健康教練，協助使用者建立飲食與生活習慣的覺察。

【使用者的目標】
{goals_list}

【今日已記錄】
{today_records}

【剛才的記錄】
{current_record}

請針對「剛才的記錄」提供簡短回饋：
1. 這個行為/餐食的評估（正向肯定或溫和提醒）
2. 與今日脈絡的關聯（如果有的話）
3. 下一步建議（具體、可執行）

【回應風格】
- 簡潔，不超過 150 字
- 語氣溫和但直接
- 著重覺察，不說教
- 用「你」稱呼使用者
```

### 5.2 週回顧（Weekly）

```
你是一位健康教練，協助使用者回顧一週的健康行為。

【使用者的目標】
{goals_list}

【過去七天的記錄】
{week_records}

【過去七天的 AI 洞察摘要】
{week_insights}

請提供週回顧：

## 本週亮點
（2-3 個做得好的行為模式）

## 可以調整的地方
（1-2 個具體的改善點）

## 下週建議
（1-2 個具體、可執行的行動）

【回應風格】
- 具體引用記錄中的例子
- 語氣像朋友間的討論
- 著重正向強化
- 總長度不超過 300 字
```

---

## 6. Module C: 週期檢視 Skill 規格

> 此模組的實作留待後續 Skill 開發工作流，以下為完整規格定義。

### 6.1 基本資訊

| 項目 | 內容 |
|------|------|
| Skill 名稱 | body-hack-review |
| 觸發詞 | 「週期檢視」「健康數據分析」「檢視我的進展」「Body Hack 分析」 |
| 輸入 | 使用者上傳從 App 匯出的 CSV + 啟動 Prompt |
| 輸出 | 對話式分析；可選產出 Markdown 摘要報告 |

### 6.2 核心功能

| 功能 | 說明 |
|------|------|
| 趨勢分析 | 辨識血糖、體重、體脂等指標的長期趨勢 |
| 目標追蹤 | 計算與目標的差距、預估達成時間 |
| 模式辨識 | 發現重複出現的行為模式（好的與壞的） |
| 異常偵測 | 標記異常數據點，詢問可能原因 |
| 策略建議 | 基於數據提出具體可執行的調整建議 |

### 6.3 對話框架

#### 開場（讀取資料後）

```
我已讀取你的健康數據，資料區間為 {date_range}，共 {record_count} 筆記錄。

根據你設定的目標：
{goals_summary}

讓我先提供一個整體概況，然後我們可以深入討論任何你感興趣的面向。

【整體趨勢】
...

【關鍵發現】
...

你想先聊哪個部分？或者有什麼具體問題？
```

#### 互動風格

| 原則 | 說明 |
|------|------|
| 教練式引導 | 不只給結論，也問問題促進反思 |
| 數據佐證 | 每個觀點都引用具體數據 |
| 正向框架 | 先肯定進步，再討論改善空間 |
| 具體可行 | 建議要具體、可立即執行 |

#### 結尾（使用者準備結束時）

```
這次討論的重點整理：

【確認的進展】
...

【需要關注的部分】
...

【下一階段行動】
1. ...
2. ...

要我把這份摘要整理成 Markdown 讓你保存嗎？
```

### 6.4 資料處理邏輯

| 步驟 | 處理內容 |
|------|----------|
| 1. 解析 CSV | 識別欄位、轉換資料型別 |
| 2. 時間對齊 | 按日期分組，建立每日摘要 |
| 3. 計算統計 | 各指標的平均、趨勢、變異 |
| 4. 關聯分析 | 事件與生理數據的時序關聯 |
| 5. 生成洞察 | 基於統計結果產出觀察 |

### 6.5 驗收標準

- [ ] 能正確解析 App 匯出的 CSV 格式
- [ ] 能識別並回應啟動 Prompt 中的目標資訊
- [ ] 趨勢分析準確（與人工檢視一致）
- [ ] 對話風格符合「教練式引導」
- [ ] 能產出結構化的 Markdown 摘要
- [ ] 處理邊界情況（資料不足、異常值）

---

## 7. 核心模組規格

### 7.1 core/sheets_client.py

| 方法 | 功能 |
|------|------|
| `__init__(credentials)` | 初始化連線 |
| `read_sheet(sheet_name, filters)` | 讀取指定 Sheet，支援篩選 |
| `append_row(sheet_name, data)` | 新增一筆記錄 |
| `update_row(sheet_name, row_id, data)` | 更新指定記錄 |
| `get_categories(sheet_name, column)` | 取得某欄位的不重複值（用於 autocomplete） |

### 7.2 core/gemini_client.py

| 方法 | 功能 |
|------|------|
| `__init__(api_key, model)` | 初始化，指定模型 |
| `ocr_image(image_bytes, prompt)` | 圖片 OCR |
| `chat(messages, system_prompt)` | 對話（用於 AI 洞察） |

### 7.3 core/data_models.py

使用 Pydantic 定義資料結構：

```python
class PhysioRecord(BaseModel):
    id: str
    datetime: str
    date: str
    time: str
    source: str
    blood_glucose: Optional[int]
    glucose_trend: Optional[str]
    weight: Optional[float]
    body_fat: Optional[float]
    systolic: Optional[int]
    diastolic: Optional[int]
    heart_rate: Optional[int]
    hrv: Optional[int]
    sleep_score: Optional[int]
    steps: Optional[int]
    floors: Optional[int]
    note: Optional[str]

class StructuredEvent(BaseModel):
    id: str
    datetime: str
    date: str
    time: str
    category: str  # meal / exercise / water
    sub_category: Optional[str]
    content: Optional[str]
    starch_level: Optional[str]
    is_alcohol: Optional[bool]
    duration_min: Optional[int]
    intensity: Optional[str]
    volume_ml: Optional[int]
    note: Optional[str]

class SimpleEvent(BaseModel):
    id: str
    datetime: str
    date: str
    time: str
    category: str
    description: Optional[str]
    note: Optional[str]

class Insight(BaseModel):
    id: str
    datetime: str
    date: str
    type: str  # realtime / weekly
    trigger_summary: str
    context_summary: str
    ai_response: str
    model: str
    note: Optional[str]

class Goal(BaseModel):
    id: str
    metric: str
    current_value: float
    target_value: float
    unit: str
    target_date: str
    status: str  # active / achieved / paused
    created_at: str
    updated_at: str
    note: Optional[str]
```

---

## 8. 錯誤處理策略

| 情境 | 處理方式 |
|------|----------|
| Google Sheets API 配額超限 | Retry with exponential backoff（最多 3 次） |
| Gemini API 配額超限 | 顯示友善提示，建議稍後再試 |
| OCR 解析失敗 | 顯示原始回應，讓使用者手動輸入 |
| 網路錯誤 | 顯示錯誤訊息，提供重試按鈕 |
| 資料驗證失敗 | 標示錯誤欄位，提示修正 |

---

## 9. 安全性考量

| 項目 | 措施 |
|------|------|
| API Keys | 透過 Streamlit Secrets 管理，不進 repo |
| 使用者資料 | 僅存於使用者自己的 Google Sheet |
| 上傳圖片 | 處理完即丟棄，不持久化 |
| 傳輸 | 全程 HTTPS |
